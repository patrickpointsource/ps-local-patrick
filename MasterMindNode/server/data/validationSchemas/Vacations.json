{
	"type":"object",
	"$schema": "http://json-schema.org/draft-04/schema",
	"id": "Vacations",
	"properties":{
		"_id": {"type":"string"},
		"_rev": {"type":"string"},
		"_deleted": {"type":"boolean"},
		"etag": {"type": ["string", "number"]},
		"form": {"enum": ["Vacations"]},
		"created": {"type": "string", "format": "date-time"},
		"startDate": {"type":"string", "format": "date-time"},
		"endDate": {"type":"string", "format": "date-time"},
		"person": { "$ref" : "#/definitions/resource"},
		"vacationManager": { "$ref" : "#/definitions/resource"},		
		"description": {"type":"string"},
		"type": {"type":"string", "prohibited_values": [""]},
		"status": {"type":"string", "prohibited_values": [""]},
		"resource": {"type":"string"},
		"about": {"type":"string"},
		"days": {"type":"string"},
		"comment": {"type":"string"}
	},
	"definitions": {
		"resource": {
			"type":"object",
			"properties":{
				"resource": {"type":"string"},
				"name": {"type":"string"}
			},
			"required": ["resource"]
		}
	},	
	"required": ["form", "person", "vacationManager", "type", "status",
		"startDate", "endDate"]
}
